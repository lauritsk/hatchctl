version: 2

before:
  hooks:
    - rm -rf internal/bridge/embedded
    - mkdir -p internal/bridge/embedded
    - CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -o internal/bridge/embedded/hatchctl-bridge-helper-linux-amd64 ./cmd/hatchctl-bridge-helper
    - CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -trimpath -o internal/bridge/embedded/hatchctl-bridge-helper-linux-arm64 ./cmd/hatchctl-bridge-helper

builds:
  - id: hatchctl
    main: ./cmd/hatchctl
    binary: hatchctl
    tags:
      - release
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/lauritsk/hatchctl/internal/version.Version={{ .Version }}
  - id: hatchctl-bridge-helper
    main: ./cmd/hatchctl-bridge-helper
    binary: hatchctl-bridge-helper-linux-{{ .Arch }}
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

checksum:
  name_template: checksums.txt

archives:
  - id: hatchctl
    ids:
      - hatchctl
  - id: hatchctl-bridge-helper
    ids:
      - hatchctl-bridge-helper
    name_template: "{{ .ProjectName }}-bridge-helper_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - --bundle=${signature}
      - ${artifact}
      - --yes
    artifacts: checksum
